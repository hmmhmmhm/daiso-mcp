# 운영 진단 유지보수 설계

사용자가 2026-09-19 전체 점검의 후속 개선을 승인했습니다. 기존 Mac 전용 계정·headed Chromium 한 탭·독립 guard·Tunnel·Access 설계를 유지합니다.

원본 오류는 직접 HTTP 계층에서 발생한 그대로 전달합니다. 유료 대체 경로는 계속 차단하며, 비활성 경로의 오류가 관측된 원인을 덮지 않게 합니다. 릴레이 설정 상태는 `/health`에 비밀 값 없이 추가하고 전송 계층의 URL 검증을 재사용합니다. 설정 유효성과 런타임 도달 가능성을 구분합니다. 모든 실패 ID가 알림의 상세 내용보다 앞에 오고 전체 진단은 Actions summary에 보존됩니다.

호환 범위 의존성을 갱신합니다. 디트릭스 포트 변경은 실제 Worker 재현이 성공할 때만 반영합니다. 브라우저 실행·SIGKILL 후 회수 시험으로 기존 안전장치를 다시 확인합니다. 관리자 인증 또는 Cloudflare 권한 없이는 다른 계정으로 우회 설치하지 않고, 독립적으로 가능한 코드 배포를 완료합니다.

완료 조건은 check·coverage 100%·build·audit 및 독립 리뷰 통과, 배포 뒤 실제 config/원본 오류/MCP 확인입니다. 외부 연결과 업스트림 장애의 미해결 부분은 별도로 남깁니다.
