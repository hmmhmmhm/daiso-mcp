# AI is off by default in the Windows program.
# Enter these values in the UI only when AI image extraction is needed.
OPENROUTER_API_KEY=
OPENROUTER_ENDPOINT=https://openrouter.ai/api/v1/chat/completions
OPENROUTER_MODEL=openai/gpt-4o-mini
